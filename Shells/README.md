# Shells

A Shell is optional presentation and input. It may add color, layout, animation, audio, accessibility, automation, remote play, or entirely different controls, but it may not contain simulation rules.

The included `Godot` Shell consumes the Kernel Host JSON-lines protocol. Other valid Shells could be a terminal REPL, a Discord bot, a screen reader, a web client, or an AI experiment harness.
